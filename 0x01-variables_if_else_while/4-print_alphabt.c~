#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char x;
if (char x != 'e' || x != 'q')
{
for (x = 'a'; x <= 'z'; x++)
{
putchar(x);
}
}
printf("\n");
return (0);
}
