#include "holberton.h"
/**
 * print_alphabet_x10 - to print alphabet ten times
 * @void: no argument
 *
 * Return: Always 0 (Success)
 *
 *
 */
void print_alphabet_x10(void)
{
char a;
char b;
for (b = 0; b <= 10; b++)
{
for (a = 'a'; a <= 'z'; a++)
{
_putchar(a);
}
_putchar('\n');
}
}
