#include "holberton.h"
/**
 * _islower - to print if lowercase 1 else 0
 * @int: for integers
 *
 * Return: Always 0 (Success)
 *
 *
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
_putchar('1');
}
else
{
_putchar('0');
}
}
